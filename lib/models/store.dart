class Store {
  String name, cep, email, id, insta, icon, cover;

  Store({
    required this.name,
    required this.cep,
    required this.email,
    required this.id,
    required this.insta,
    required this.icon,
    required this.cover,
  });
}
